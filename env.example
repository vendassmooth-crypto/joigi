# Token do bot (Discord Developer Portal > Bot > Token)
TOKEN=

# Prefixo dos comandos de texto (padrão: !)
PREFIX=!

# Porta usada pelo servidor de keepalive (o Railway define isso sozinho, pode deixar)
PORT=3000
